<%= form_tag(tools_path, method: "get", class: "form-inline") do %>
	<div class="form-group mb-2">
	  <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Buscar herramientas" %>
	</div>
	<%= submit_tag "Search", class: "btn btn-secondary mb-2" %>
  <% end %>
